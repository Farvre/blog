# 📖 博客使用说明（写给不懂代码的你）

这个博客网站已搭建完成，下面用大白话教你日常怎么用。

---

## 一、发一篇新文章（3 步，约 1 分钟）

### 方式A：用「发帖小助手」（推荐，最省事）

1. 打开电脑的**终端/命令行**（git-bash 或 VSCode 终端）
2. 进入项目目录，输入：`npm run new-post`
3. 按提示回答「标题 / 描述 / 标签 / 封面」，脚本会自动生成一个帖子文件

生成的帖子在 `src/content/posts/` 文件夹里，用**记事本/任意文本编辑器**打开，把正文写好，保存。

### 方式B：直接复制模板（熟悉后更快）

在 `src/content/posts/` 里复制任意一个 `.md` 文件，改文件名，改里面的标题和内容，保存。

---

## 二、各种内容怎么写？

打开一个 `.md` 帖子文件，中间就是正文。规则很简单：

### 文字
直接打字就是正文。空一行就是另起一段。

```
**加粗**
*斜体*
# 大标题
## 二级标题
```

### 图片
1. 把图片文件放进 `public/uploads/` 文件夹（网站根目录的 `public/uploads`）
2. 在正文里写：`![图片说明](/uploads/图片文件名.jpg)`

### 视频（长视频）
推荐传到 **B站** 或 **YouTube**，复制页面的「嵌入代码」（iframe），粘贴进正文。不占自己空间、加载也快。

### 视频（短视频）
把 mp4 放进 `public/uploads/`，正文里写：
```
<video controls src="/uploads/视频名.mp4"></video>
```

---

## 三、发布上线

改完帖子保存后，把代码推到 GitHub（或用一键部署脚本），网站就会自动更新。

> 具体部署命令，等部署方案确定后，我会写进这里 / 或放进项目根目录的 `部署.md`。

---

## 四、评论和点赞

- **点赞**：读者点一下 👍，浏览器自动记录，无需配置。
- **评论**：默认使用 **Waline**（免登录，填昵称+邮箱就能评论，有回复功能）。
  - 评论需要配置一个「评论服务器」（免费），配置方法见 `部署.md`。
  - 想换 GitHub 登录的 **Giscus** 也预留了开关，切换容易。

---

## 五、常见问题

| 问题 | 解决办法 |
|------|---------|
| 帖子发了但首页没显示 | 检查顶部 `draft:` 是不是 `false`；保存后重新部署 |
| 图片不显示 | 确认图片在 `public/uploads/`，且路径写法没错（/uploads/开头） |
| 想改网站标题/名称 | 打开 `src/layouts/Layout.astro`，改 `siteTitle` |

有问题随时找 **shuangyun 的 AI 助手** 帮你改。
