# Netlify 部署配置
# 构建命令和输出目录，Netlify 会自动读取

[build]
  command = "npm run build"
  publish = "dist"

# Decap CMS 是静态页 + 直接请求 /admin/config.yml，Netlify 静态服务原生支持，
# 无需 SPA 通配重定向（避免通配符把真实文件 config.yml 拦走）。
# 若未来遇到 /admin/ 下 404，可在此增加精确回退。
